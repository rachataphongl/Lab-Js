function min(x, y) {
    //1
    // if(x < y) {
    //     return x;
    // } else{
    //     return y
    // }

    //2
    if(x < y) {
        return x;
    }
        return y
}
let result = min(55, 99);
alert(result);