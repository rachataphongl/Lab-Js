//*
// function จะหา message ใน function ก่อน ถ้าเจอ     จะใช้ค่านั้น

//**
//logMessage(message) มีparam ชื่อนี้จริงแต่ซ้ำกับ global เลยเรียกตัว global

//*** 
// "John" เพราะว่า มี console.log(name) ใน function ก่อนที่จะกำหนดค่าใหม่ให้name = input 

//undifined เพราะ name ได้กำหนดค่าใหม่เป็น input แต่ input argument ที่เรียกใช้แล้ว แต่ยังไม่ได้ใส่ค่าลงไปเลย
